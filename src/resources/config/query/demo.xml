<?xml version="1.0" encoding="UTF-8" ?>

<mapper namespace="demo"  >
	<sql id="allMers"> 
	<![CDATA[
		 select * from umpay.t_mer_inf mer  where 1=1
		 #if( $merId && $merId!='' ) 
		   and  mer.merId like '%$merId%'   
		 #end 
		 #if( $merName && $merName!='' ) 
		   and  mer.mername like '%$merName%'   
		 #end 
		  #if( $state && $state!='' ) 
		   and  mer.state=:state 
		 #end
		 order by mer.intime desc
	  ]]>
	</sql>
	   <sql id="allMersToAudit"> 
	<![CDATA[
		 select * from umpay.t_mer_inf mer  where 1=1
		 #if( $merId && $merId!='' ) 
		   and  mer.merId like '%$merId%'   
		 #end 
		 #if( $merName && $merName!='' ) 
		   and  mer.merName like '%$merName%'   
		 #end 
		  #if( $state && $state!='' ) 
		   and  mer.state=:state 
		 #end
		 order by mer.intime desc
	  ]]>
	</sql>
</mapper> 