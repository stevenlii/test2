<?xml version="1.0" encoding="UTF-8" ?>
<mapper namespace="couponmertel">
	<sql id="all">
	<![CDATA[
		select merTelId,
		       merId,
		       linkTel,
		       exchangeType,
		       remark,
		       state,
		       modUser,
		       modTime,
		       inTime
		  from umpay.T_COUPON_MERTEL
		 where 1 = 1
		   #if( $merId && $merId!='' ) 
		       and merId = :merId
		   #end
		   #if( $linkTel && $linkTel!='' ) 
		       and linkTel = :linkTel
		   #end
		 order by inTime desc
	]]>
	</sql>

</mapper> 